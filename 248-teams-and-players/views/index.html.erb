<h1>List of teams</h1>

<ul>
  <% @teams.each do |team| %>
    <li>
      <a href="/teams/<%= team.name %>">
      <%= team.team_id.name %>
        is on th<%= team.name %>
      </a>
    </li>
  <% end %>
</ul>

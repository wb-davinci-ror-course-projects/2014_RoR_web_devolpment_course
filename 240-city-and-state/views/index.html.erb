<h1>Largest U.S. cities</h1>

<ul>
  <% @cities.each do |city| %>
    <li>
      <a href="/cities/<%= city.id %>">
        <%= city.rank %>.
        <%= city.name %>, <%= city.state_abbreviation %>
        (population <%= city.population %>)
      </a>
    </li>
  <% end %>
</ul>
